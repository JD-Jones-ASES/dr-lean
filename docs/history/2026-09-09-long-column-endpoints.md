# Long-column endpoints and all aspect ratios, 2026-09-09

Nineteen of the twenty intended release targets are proved locally. Four
new principal results supply the complete accepted long-column ranges and
the large-row theorem for every aspect ratio, each with transpose and
unique uniform equality on the full closed probability simplex.

The [quadratic strip](../ENDPOINT-QUADRATIC.md) holds for m>=96 and
N>=10000m^2. The [quartic strip](../ENDPOINT-QUARTIC.md) holds for m>=16 and
N>=20000m^4. The [combined strip](../ENDPOINT-COMBINED.md) holds for m>=5
and N>=10^11m^2. The [all-aspect theorem](../ENDPOINT-ALL-ASPECTS.md) covers
every N>=m when m>=10^18 by proving that four established intervals overlap.

The actual leading event and column-averaging comparison retain original
row union bounds, factorial factors, and centered column costs whose total
need not be one. Quantitative coarse and saturated gauge estimates at true
compact minima yield row concentration. The resulting actual contender
comparison absorbs the column variance and proves every column mass is
less than 25/N. Deletion then produces positive retained kernels, with the
retained-row avoidance probability kept until its own collision bound.
For m=5 through 15, a literal repeated-cell event supplies the initial cap;
a sharper half-factorial elementary bound removes the exponential loss in
the retained coefficient. No narrower family is substituted for the full
real probability domain.

The checkpoint also includes the near-endpoint finite cut certificates,
scalar two-zero equalization and bracket estimates, and the infinite
logarithmic gap. Actual permanent-face stationarity and strict support
expansion at a genuine face minimum are proved internally using an exact
competitor, block floors and Hall matching. Repeated-support averaging and
the arbitrary-matrix reduction remain separate active obligations; the
square near-endpoint principal target is still pending.

Every included new production chain and persistent test package received
independent semantic review. The final combined build, axiom audit, frozen
source hashes, isolated source-package controls and local link counts are
recorded in [PROGRESS](../PROGRESS.md). Active downstream modules are excluded.
The unchanged historical full K3 cold replay was not repeated; its exact
sources and regeneration/corruption controls remain part of verification.

Independent CI for the exact default-branch commit, the final principal
proof, Challenge/Solution, Comparator and NanoDa are still required before
publication. The repository remains private. Full arbitrary-rectangle
Rybin P2 remains open. JD will manually submit the completed release to Palomar.
