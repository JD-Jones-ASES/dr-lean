# Fixed-board and endpoint checkpoint, 2026-09-09

The two fixed-board K=4 theorems are now complete locally, including the
sharp constants and iff uniform equality on the closed probability simplex.
The complete four-row K=4 and LLL endpoint strip have also passed.
This brings the required principal inventory to eight proved targets out of
twenty. Full arbitrary-rectangle P2 remains open.

The [fixed-board proof](../FIXED-BOARD-ORDER-FOUR.md) connects all 20 literal
weighted kernels and 80 strict Gram blocks to the actual signed quintic
identity, with 300 positive pivots. Physical seed transport retains actual
host labels and all ordinary multiplicities. The tests prove strict gaps
for boards with zero cells and reject incorrect sharp constants and
unweighted compressed kernels.

The endpoint additions prove exact deleted-row rook normalization and its
actual averaging-kernel conjugacy, quantitative row-product concentration,
the true uniform collision-avoidance exponential bound, retained-row
balance, and localized collision-kernel bounds. The elementary lower bound
keeps its true factorial and all zero coordinates. One-sided column rigidity
now closes the endpoint equality proof. The remaining parameter ranges
still require their own actual matrix and global-maximizer arguments.

The square order-four coefficient file active at an earlier CI termination
is split into fourteen serialized chunks. Its 448 literal gates and two
dispatchers retain exactly their earlier theorem statements and proof bodies.
All chunks and the affected square theorems were replayed. Exit code 143
does not identify the earlier termination cause, which remains unconfirmed.

The combined `lake --wfail build +DR +Test` passed **4,559 jobs** and audited
**38,776 project declarations**, allowing only `propext`, `Classical.choice`
and `Quot.sound`. The 1,107 curated Lean source hashes were captured before
the build and verified unchanged after it. No warnings were emitted.
Independent review covered the fixed-board soundness, actual host transport,
kernel polynomial evaluation, endpoint arguments, generator and meaningful
negative controls. The new exact generation and controls replay normally
and under Python optimization.

The larger K3 case replay and the remaining endpoint assemblies
remain separate active work. They are excluded from this curated source
closure. Private main and the durable checkout remain at independently
accepted `71ab5fb1e4bae27fbd43f26ab06f2e1646750836`; later local proofs still
require exact-commit independent CI and default-branch verification.
The repository remains private. The full release gate and JD's manual
Palomar intake remain unchanged.
