# Quintic and endpoint development checkpoint, 2026-09-09

This private checkpoint completes additional proof dependencies. The principal
release inventory remains four proved targets out of twenty. Arbitrary P2
and all unfinished release targets retain their stated open or pending status.

## Mathematical additions

- Signed iid quintic symmetrization, repeated-cell weights, all 2,704 physical
  pattern gates, exact sparse coefficient rows and the sharp probability
  adapters for the four-row families and both fixed seeds.
- Actual host permutations and ten physical multiplier seeds, including all
  repeated tuples; two-axis decomposition with exact aggregate weights and
  constant kernels, retaining absent sectors when an ordinary count is one.
- All 68 four-row small block chains: 680 exact Bernstein matrices and
  2,760 positive Gram pivots, connected to actual cleared role entries.
- Independent-row collision bounds, conditional finite local lemma,
  collision-class ratios, exhaustive deletion patterns, exact participation
  masses and the expected deletion-matrix lower bound in localized loads.
- Scalar and actual matrix square completion with the exact 3/32 margin;
  no assumption that the desired endpoint matrix is positive is introduced.

## Local verification

The stable combined `lake --wfail build +DR +Test` passed 4,433 jobs with
32,030 project declarations audited; only `propext`, `Classical.choice`,
and `Quot.sound` are allowed. All 982 curated Lean source hashes remained
unchanged across that build. Every production dependency of a test is also
reachable from DR, so the global axiom audit covers it.

Normal and optimized Python runs reproduced the full quintic template,
all pattern-gate modules, both fixed seeds, all four-row source data and
all 68 Gram/transform/physical block chains. The fixed-seed controls reject
28 corruptions in eight groups; the generic quintic controls reject 27 in
ten groups. Thirteen bounded-scheduler controls and seven source-guard
controls passed in both Python modes. Persistent Lean tests include signed,
zero-mass, repeated-cell, padding, absent-sector and exact-boundary cases.

Two integration failures were corrected before the stable build: a helper
name collided between the square and endpoint namespaces, and a new test
briefly reused an earlier untracked test filename. The endpoint helper was
renamed; the earlier test was restored byte-for-byte and the new test was
renamed separately. The successful combined build above follows both fixes.

## Independent verification and scope

Private main and the durable checkout remain at independently verified
`71ab5fb1e4bae27fbd43f26ab06f2e1646750836`. Later exit-143 and cancelled
runs are not accepted. Their termination cause remains unconfirmed.
This checkpoint raises the independent job ceiling to 360 minutes for the
expanded serialized replay; all proof checks and exact tracked coverage
remain required. Independent success on the exact checkpoint is still a
separate obligation, followed by default-branch verification.

The broader 1,330-case K3 replay continues outside this curated checkpoint.
The working replay has completed the 954-case four-row prefix and all its
60 dispatch shards. This checkpoint retains the previously curated seventeen
case files; the full generated catalogue will be assembled separately.
The four-row K4, fixed K4 and endpoint assembly work also continues separately.
No public visibility change or Palomar submission is made by this increment.
