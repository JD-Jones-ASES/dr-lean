# Complete low orders and endpoint ranges, 2026-09-09

Fourteen of the twenty intended principal release targets are now proved
locally. This increment completes K=3 on every admissible rectangle, the
small-side and all-order 5×5 corollaries, the logarithmic arithmetic endpoint
range, the full m≥117 short range and the m≥80 doubled range. All statements
retain the closed probability simplex, sharp uniform value and iff equality.

The [K=3 assembly](../ORDER-THREE-COMPLETE.md) follows the complete 1,330-case,
87-shard replay. The original case scheduler and dispatch follower both
finished successfully. Canonical coverage, all literal equations, aggregate
kernels and strict blocks were checked; generated files reproduce under
ordinary and optimized Python. The [receipt](../../data/FINITE_K3_COMPLETE_REPLAY.md)
retains the exact coverage counts and source/log hashes.

The [arithmetic scaling proof](../ENDPOINT-BOUNDARY-SCALING.md) derives
the full shared subset discrepancy from actual contender deficits, constructs
balanced domination through exact real transport, and applies the internally
proved boundary permanent floor to the actual rectangular padding. Bernoulli
and square completion exclude every boundary contender, including zero
deficit. The compact maximum-norm argument then gives global unique equality.
The [near-square and doubled criteria](../ENDPOINT_NEAR_DOUBLE.md) use exact
factorial recurrences, common polynomial-weight bounds, two rational bases
and genuine all-integer induction.

The exact [transition endpoint theorem](../ENDPOINT-TRANSITION-PROOF.md) is
also complete, with original-row avoidance and deleted-row avoidance kept
separate. It is a component of the pending all-aspect theorem. The
[collision-cluster kernel bound](../ENDPOINT-COLLISION-CLUSTER.md) supplies
a further proved input for the long-column strips. The remaining large
endpoint, quadratic, quartic, combined, consecutive and square near-endpoint
release targets are still pending. Full arbitrary-rectangle P2 remains open.

The combined `lake --wfail build +DR +Test` passed **6,021 jobs** and audited
**195,889 project declarations**, allowing only `propext`, `Classical.choice`
and `Quot.sound`. All **2,569 included Lean source hashes** were captured
before the build and verified unchanged afterward. No warnings were emitted.
Independent semantic reviews covered all new mathematical chains and their
boundary, equality and rejected-hypothesis controls.

Active leading-gauge, zero-rectangle and minimum-dilation work is excluded
from this checkpoint's import closure. No partially built proof is needed
by a completed result. The isolated source package replays the generator and
control checks; strict tracked coverage requires every included production
module to enter the global axiom audit.

This is a private development checkpoint. Independent exact-commit CI and
the default-branch gate remain required. The complete Challenge/Solution,
Comparator and NanoDa release gate has not run, and the repository has not
been made public. JD's later manual Palomar submission remains unchanged.
