import DR.Certificates.FiniteK4OrbitData
import DR.Certificates.FiniteK4RoleCache

/-! Generated literal role-formula checks. Each row is checked separately
by Lean's kernel; the final theorem covers all 2704 pattern pairs. -/

namespace DittertRybin.Certificates
set_option maxRecDepth 10000
set_option maxHeartbeats 0

private theorem finiteK4RoleCheck00 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 0 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 0).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck01 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 1 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 1).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck02 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 2 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 2).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck03 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 3 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 3).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck04 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 4 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 4).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck05 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 5 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 5).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck06 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 6 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 6).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck07 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 7 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 7).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck08 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 8 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 8).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck09 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 9 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 9).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck10 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 10 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 10).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck11 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 11 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 11).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck12 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 12 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 12).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck13 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 13 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 13).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck14 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 14 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 14).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck15 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 15 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 15).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck16 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 16 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 16).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck17 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 17 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 17).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck18 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 18 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 18).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck19 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 19 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 19).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck20 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 20 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 20).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck21 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 21 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 21).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck22 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 22 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 22).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck23 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 23 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 23).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck24 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 24 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 24).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck25 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 25 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 25).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck26 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 26 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 26).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck27 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 27 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 27).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck28 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 28 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 28).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck29 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 29 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 29).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck30 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 30 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 30).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck31 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 31 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 31).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck32 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 32 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 32).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck33 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 33 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 33).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck34 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 34 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 34).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck35 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 35 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 35).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck36 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 36 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 36).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck37 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 37 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 37).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck38 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 38 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 38).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck39 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 39 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 39).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck40 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 40 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 40).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck41 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 41 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 41).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck42 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 42 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 42).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck43 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 43 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 43).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck44 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 44 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 44).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck45 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 45 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 45).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck46 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 46 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 46).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck47 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 47 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 47).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck48 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 48 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 48).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck49 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 49 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 49).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck50 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 50 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 50).get c) := by
  decide +kernel

private theorem finiteK4RoleCheck51 : ∀ c : Fin 52,
    finiteK4CachedRoleKey 51 c =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get 51).get c) := by
  decide +kernel

theorem finiteK4RoleTable_correct (r c : Fin 52) :
    finiteK4TupleRoleKey (finiteK4PatternCell r c) =
      finiteK4RoleKeys.get ((finiteK4RoleTable.get r).get c) := by
  rw [finiteK4TupleRoleKey_pattern_cache]
  fin_cases r
  · exact finiteK4RoleCheck00 c
  · exact finiteK4RoleCheck01 c
  · exact finiteK4RoleCheck02 c
  · exact finiteK4RoleCheck03 c
  · exact finiteK4RoleCheck04 c
  · exact finiteK4RoleCheck05 c
  · exact finiteK4RoleCheck06 c
  · exact finiteK4RoleCheck07 c
  · exact finiteK4RoleCheck08 c
  · exact finiteK4RoleCheck09 c
  · exact finiteK4RoleCheck10 c
  · exact finiteK4RoleCheck11 c
  · exact finiteK4RoleCheck12 c
  · exact finiteK4RoleCheck13 c
  · exact finiteK4RoleCheck14 c
  · exact finiteK4RoleCheck15 c
  · exact finiteK4RoleCheck16 c
  · exact finiteK4RoleCheck17 c
  · exact finiteK4RoleCheck18 c
  · exact finiteK4RoleCheck19 c
  · exact finiteK4RoleCheck20 c
  · exact finiteK4RoleCheck21 c
  · exact finiteK4RoleCheck22 c
  · exact finiteK4RoleCheck23 c
  · exact finiteK4RoleCheck24 c
  · exact finiteK4RoleCheck25 c
  · exact finiteK4RoleCheck26 c
  · exact finiteK4RoleCheck27 c
  · exact finiteK4RoleCheck28 c
  · exact finiteK4RoleCheck29 c
  · exact finiteK4RoleCheck30 c
  · exact finiteK4RoleCheck31 c
  · exact finiteK4RoleCheck32 c
  · exact finiteK4RoleCheck33 c
  · exact finiteK4RoleCheck34 c
  · exact finiteK4RoleCheck35 c
  · exact finiteK4RoleCheck36 c
  · exact finiteK4RoleCheck37 c
  · exact finiteK4RoleCheck38 c
  · exact finiteK4RoleCheck39 c
  · exact finiteK4RoleCheck40 c
  · exact finiteK4RoleCheck41 c
  · exact finiteK4RoleCheck42 c
  · exact finiteK4RoleCheck43 c
  · exact finiteK4RoleCheck44 c
  · exact finiteK4RoleCheck45 c
  · exact finiteK4RoleCheck46 c
  · exact finiteK4RoleCheck47 c
  · exact finiteK4RoleCheck48 c
  · exact finiteK4RoleCheck49 c
  · exact finiteK4RoleCheck50 c
  · exact finiteK4RoleCheck51 c

end DittertRybin.Certificates
